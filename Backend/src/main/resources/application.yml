spring:
  datasource:
    url: jdbc:postgresql://db:5432/vibecheck
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true
server:
  port: 8080